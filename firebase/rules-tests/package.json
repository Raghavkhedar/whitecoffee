{
  "name": "whitecoffee-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Security-rule tests for firebase/firestore.rules, run against the Firestore emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project whitecoffee-rules-test \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
