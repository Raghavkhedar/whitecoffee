# Secrets (never commit)
keystore.properties
**/keystore.properties
*.jks
*.keystore
**/local.properties
admin/.env.local
**/.env.local
firebase/functions/*.local
# Service-account keys (belt-and-suspenders — never commit these anywhere)
**/serviceAccount*.json
**/*-serviceaccount*.json
**/wckey.json
# Rollback backups written by purge-inline-pay.js — every employee's salary, in plaintext.
# The whole point of that migration is that pay stops being readable by whoever can read a
# user record; committing the backup would put it in every clone of the repo instead.
firebase/scripts/inline-pay-backup-*.json

# Android / Gradle
android/.gradle/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
# Build artifacts — regenerable, keep them out of git
*.apk
*.aab
android/app/debug/
android/app/release/

# One-off design handoff bundle — not part of the build
android/WhiteCoffe.zip

# Node / Next.js
admin/node_modules/
admin/.next/
admin/out/
firebase/functions/node_modules/

# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log

# Graphify (regenerated at root)
graphify-out/
